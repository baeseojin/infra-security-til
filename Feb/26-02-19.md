## 📝 오늘 배운 내용 요약

1. SSH (secure shell)
    - SSH는 비대칭키 암호화 방식과 대칭키 암호화 알고리즘을 동시에 사용한다.
        - 대칭키 알고리즘
            - 데이터를 암호화 할 때 사용하는 키와 복호화 할 때 사용하는 키가 같은 알고리즘이다.
            - 사용되는 키는 비밀키
        - 비대칭키 알고리즘
            - 데이터를 암호화 하고 복호화 할 때 사용하는 키가 다른 암호화 알고리즘 이다.
            - 공개키
                - 외부에 공개된 키로 누구나 공개키를 가지고 있어도 된다.
                공개키는 데이터를 암호화 하여 전달하며 공개키와 쌍으로 이루어진 개인키를 이용해 암호화된 데이터를 복호화 한다.
            - 개인키
                - 키 생성자만 갖고 있는 키.
            - 공개키로 암호화 -> 개인키로 복호화
    - 계정 명을 입력 안 하면 현재 내가 로그인한 계정 명으로 접속
    - `ssh [계정명]@[주소] [command]`
        - 일회성으로 해당 접속해 명령어 실행
    - SSH 인증
        - SSH 접속 - 그냥 접속
            - 시스템에 원격으로 접속할 때 기본적으로 사용자 아이디와 패스워드를 알고 있어야 한다.
        - SSH 접속 - 키 기반 인증
            - 사용자의 암호를 알고 있지 않아도 원격 접속 할 수 있는 인증 방법이다.
            - 키 기반 인증을 위해 키를 등록하려면 해당 사용자의 패스워드를 알아야 한다.
            - `ssh-keygen` : 키 기반 인증을 위한 공개키와 개인키 생성
    - `ssh-copy-id id@server ip address`   : 클라이언트가 생성한 공개키를 서버에 등록하게 됩니다.
    - SSH 동작 순서
    
    ```powershell
    1. SSH 접속
    SSH 서버의 공개키를 저장하기 위한 메세지가 출력
    Server(대기) & Client(접속)
    
    2. client 가 server 접속요청
    
    3. server가 client 공개키 전송
    	서버의 공개키가 클라이언트에 저장되어 있지 않으면 
    	SSH 서버의 공개키를 저장하기 위한 메세지가 출력된다.
    4. client : 비밀키 생성
    	공개키를 저장하고 난 다음 Client는 대칭키 알고리즘을 사용해 
    	암호화를 위한 비밀키를 생성한다.
    
    5. client : 비밀키 암호화 (서버의 공개키)
    	이 비밀키를 다시 서버의 공개키로 암호화 하여 서버에게 전달한다.
    6. client : 비밀키 전송
    
    7. server : 비밀키 복호화 (서버의 개인키)
    ```
    
2. 리눅스 방화벽
    - 외부에서 시스템으로 접근하는 패킷을 차단하는 서비스
    - 규칙을 이용해 접근을 허용하거나 차단할 수 있다.
        - zone
        - 서비스
        - 포트
        - IP
        - interface
    - Netfilter(Kernel)
        - iptables : Netfilter 제어하는 도구를 사용 (Centos 7이전)
        - firewall : iptables의 단점을 보완. 동적으로 방화벽 설정 변경 가능
            - Runtime	: 시스템 운영중인 상태일때 반영.
            - Permanent	: 서비스가 재시작 되었을때도 반영. XML파일
        - pre-defined Zone
            - block
                - 모든 패킷 거부
            - dmz
                - 내부로 들어오는 패킷 거부
            - drop
                - 내부로 들어오는 모든 패킷 DROP 폐기, ICMP 에러도 폐기
                - 폐기(drop) : 패킷을 받으면 응답 없이 버린다.
                - 거절(reject) : 패킷을 받으면 거부한다라는 ICMP 패킷을 보내준다.
            - external
                - 내부로 들어오는 패킷 거부 (SSH만 제외)
                - 외부로 나가는 모든 패킷 허용
            - home
                - 내부로 들어오는 패킷 거부
                - SSH, DHCP Client, samba, ...
                - 외부로 나가는 모든 패킷 허용
            - internal
                - 내부로 들어오는 패킷 거부
                - SSH, DHCP Client, samba, …
                - 외부로 나가는 모든 패킷 허용
            - public
                - Default Zone
                - 내부로 들어오는 모든 패킷 거부
                - SSH, DHCPv6-... 제외
                - 외부로 나가는 모든 패킷 허용
            - trusted
                - 내부로 들어오는 모든 패킷 허용
                - 외부로 나가는 모든 패킷 허용
            - work
                - 내부로 들어오는 모든 패킷 거부
                - SSH,DHCPv4, IPP 허용하거나 외부로 나가는 모든 패킷 허용
        - `firewall-cmd`
            - `--state`
                - firewalld 실행 상태 확인
            - `--get-default-zone`
                - 현재 기본 영역 표시
            - `--set-default-zone [zone]`
                - 기본 영역 설정
            - `--get-zones`
                - 사용 가능한 모든 영역 나열
            - `--get-services`
                - 사용 가능한 모든 서비스 나열
            - `--get-active-zones`
                - 현재 사용 중인 모든 영역과 인터페이스 및 소스 정보 나열
            - `--add-source=[ip주소] --zone=[zone]`
                - 출발지 주소 규칙 추가
                - '--zone' 옵션을 통해 zone 지정해주지 않으면 자동으로 기본영역에 추가
            - `--remove-source=[ip주소]`
                - IP 주소를 지정된 영역에서 제거
            - `--add-interface=[ifname] --zone=[zone]`
                - 특정 영역에 interface 연결 추가
            - `--change-interface=[ifname] --zone=[zone]`
                - 영역에 연결된 interface 변경
            - `--list-all --zone=[zone]`
                - 영역에 구성된 모든 인터페이스, 소스, 서비스, 포트 나열
            - `--add-service=[service] --zone=[zone]`
                - 해당 서비스 트래픽 허용
            - `--add-port=[port|protocol] --zone=[zone]`
                - 해당 포트나 프로토콜 트래픽 허용
            - `+ --permanent`
                - 해당 옵션을 사용하지 않으면 현재 설정이 변경되며 영구설정은 지정이 안됨.
            - `--reload`
                - 런타임 구성 삭제, 영구 구성 적용
            - `--runtime-to-permanent`
                - 실행중 설정을 영구 설정으로 변경

## * 지난 시간 배운 것에 대한 실습

```powershell
<네트워크 관리 실습>

초기설정으로 초기화

현재 각 인터페이스 별 IP 주소 확인(2가지 방법 모두 실행)

ifconfig

ip addr

인터페이스 별로 확인

ifconfig ens224

ip addr show ens224

- 연결 생성

유동 아이피 설정

**********************
con-name  : dhcp1 
ifname : ens224
유동 IP 설정
**********************

nmcli con add con-name dhcp1 type ethernet ifname ens224

nmcli con

ip addr

nmcli con up dhcp1

ip addr

고정 아이피 설정

**********************
con-name  : static1
ifname : ens224
IP address: 192.168.100.200
gateway : 192.168.100.2
PREFIX    : 24
**********************

nmcli con add con-name static1 type ethernet ifname ens224 
ip4 192.168.100.200/24 gw4 192.168.100.2

nmcli con

ip addr show ens224

nmcli con up static1

ip addr show ens224

- 연결 수정

dhcp1 연결 설정 수정

**********************
con-name  : static100
ifname : ens224
IP address: 192.168.100.150
gateway : 192.168.100.2
PREFIX    : 24
DNS       : 8.8.8.8
**********************

nmcli con mod dhcp1  connection.id static100  ipv4.addresses 192.168.100.150/24 ipv4.method manual ipv4.gateway 192.168.100.2 ipv4.dns 8.8.8.8

nmcli con 

ip addr show ens224

nmcli con up static100

ip addr show ens224

- 호스트네임 설정

***************************
hostname : servera.est.org
****************************

hostnamectl hostname servera.est.org

hostname

cat /etc/hostname

bash

-------------------------------------

client 컴퓨터 네트워크 설정

***************************
ip : 192.168.100.210
prefix: 24
ifname : ens224
hostname : clienta.est.org
***************************

nmcli con add con-name client ifname ens224 type ethernet ip4 192.168.100.210/24 gw4 192.168.100.2 

nmcli con

ip addr show ens224

nmcli con up client

ip addr show ens224

hostnamectl hostname clienta.est.org

hostname

cat /etc/hostname

bash
```

## * 이번 시간 배운 것에 대한 실습

```powershell
<SSH 실습>

client 
스냅샷 nic 추가로 초기화

ens224 => ip : 192.168.100.200/24 고정아이피 설정

client 에 cuser 생성하고 활성화 시키시오.

useradd cuser
passwd cuser

server
스냅샷 nic 추가로 초기화

ens224 => ip : 192.168.100.150/24 고정아이피 설정

server suser 생성하고 활성화 시키시오.

useradd suser
passwd suser

cuser 에서 suser로 ssh 접속
server : 192.168.100.150로 가정

ssh suser@192.168.100.150

exit

cuser 측에서 보관하고 있는 공개키(public key)와 suser가 보유한 공개키(public key) 내용 확인

client 

vi ~/.ssh/known_hosts

server

vi /etc/ssh/ssh_host_ed25519_key.pub

client 와 server의 내용을 보면 일치

server의 공개키와 개인키 삭제 후 sshd 재실행

server

rm -rf /etc/ssh/ssh_host*
ls /etc/ssh/
systemctl restart sshd
ls /etc/ssh/

cuser 측에서 보관하고 있는 공개키(public key)와 server가 보유한 공개키(public key) 내용 확인

client 

vi ~/.ssh/known_hosts

server

vi /etc/ssh/ssh_host_rsa.pub

client 와 server의 내용을 보면 일치X

cuser가 suser로 접속할 수 있도록 하시오.

client

rm -rf ~/.ssh/know_host*

ssh suser@192.168.100.150

exit

cuser가 suser로 암호없이 로그인 되도록 키인증방식으로 설정하시오.

client

ssh-keygen
<enter>
<enter>
<enter>
ssh-copy-id suser@192.168.100.150
suser password 입력

server의 root 계정으로 접속 할 수 없도록 설정하시오.

server

vi /etc/ssh/sshd_config

/Permit

PermitRootLogin no

------------------------------------------------------------------------

<firewalld 실습>

server 초기화

server에서 작업

firewalld  서비스 상태 확인 및 시작

systemctl status firewalld

systemctl start firewalld

httpd 패키지 설치

yum -y install httpd

nfs 서비스를 시작,활성화 후에 상태 확인

systemctl status httpd

systemctl enable httpd --now

home zone에 다음 사항 추가 영구설정

포트 번호
80/tcp, 111/tcp, 2049/udp, 20048/tcp

firewall-cmd --add-port=80/tcp --add-port=111/tcp --add-port=2049/udp --add-port=20048/tcp --zone=home --permanent

firewall-cmd --reload

프로토콜
ICMP

firewall-cmd --add-protocol=ICMP  --zone=home --permanent

firewall-cmd --reload

서비스
http,https

firewall-cmd --add-service=http --add-service=https  --zone=home --permanent

firewall-cmd --reload

home zone 상태 확인

firewall-cmd --list-all  --zone=home 

home zone 을 default-zone 으로 설정

firewall-cmd --set-default-zone home
```

## 💭 오늘의 회고

1. **배운 점**
    - `dnf install -y bash-completion` → 편의성 툴 다운(tab기능)
    - telnet : 암호화 x (평문전송)
    - ssh : 암호화 o
    - `scp [파일] [주소:[경로]]` : 파일 경로 원격 접속에 있는 파일 경로를 입력하면 원격 접속한 파일을 내 컴퓨터로도 받아올 수 있음
2. **어려운 점/개선할 점**
    - 
3. **액션 플랜**
    - 
4. **함께 나누고 싶은 점**
    - 

## 📚 참고자료

📥 [리눅스 어드민.pdf](https://raw.githubusercontent.com/baeseojin/infra-security-til/main/files/리눅스%20어드민.pdf)

## 🔍 내일 학습 예정

-
